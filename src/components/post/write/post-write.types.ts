export type WriteTab = "selected" | "library";

export type PhotoDraft = {
  description: string;
  cameraBrand: string;
  cameraModel: string;
  lensBrand: string;
  lensModel: string;
  iso: string;
  shutterSpeed: string;
  aperture: string;
  focalLength: string;
};
